import type { PortalBundle } from '../shared'

/**
 * Raw bundle returned by `get_wms_bundle`. Currently a placeholder
 * shape — additional fields will be added as the WMS overview page
 * is built out against this single round-trip.
 */
export type WmsBundle = PortalBundle
